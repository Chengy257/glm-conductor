DISJOINT-A

Fixture for WF-10 (disjoint parallel writes). Probe agents replace this file's content with
`DISJOINT-A` plus a timestamp under the run-local evidence directory. This template is the
committed canonical copy for reproducibility; the live copy is created at run time.
